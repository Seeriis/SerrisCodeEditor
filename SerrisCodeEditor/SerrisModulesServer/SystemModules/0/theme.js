/*
    ================
    = MONACO THEME =
    ================
*/

monaco.editor.defineTheme('lightTheme', {
    base: 'vs', // can also be vs-dark or hc-black
    inherit: true,
    rules: [{ background: 'efefef' }]
});